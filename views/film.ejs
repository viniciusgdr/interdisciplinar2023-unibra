<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= film.name %></title>
</head>
<body>
  <% if (film) { %>
    <h2>Titulo: <%= film.name %></h2>
    <p>Descrição: <%= film.description %></p>
    <img src="<%= film.picture_url %>" alt="" width="200" height="400">

    <h2>Sessões</h2>
    <ul>
      <% for (let i = 0; i < sessions.length; i++) { %>
        <li>
          <p>Data: <%= sessions[i].date %> (<%= sessions[i].hour %>)</p>
          <p>Assentos Disponíveis: <%= sessions[i].available_seats %></p>
          <a href="/checkout/?filmId=<%= film.id %>&sessionId=<%= sessions[i].id %>">Comprar</a>
        </li>
      <% } %>
    </ul>
  <% } else { %>
    <p>Not found</p>
  <% } %>
</body>
</html>